import React from "react";

const Services = (props) => {
    return (
        <>
            <h1>This is {props.title} page</h1> {/*here we're using props to show the content in this component*/}
        </>
    )
}

export default Services;